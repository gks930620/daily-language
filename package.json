{
  "name": "daily-language",
  "version": "1.0.0",
  "description": "매일 아침 영어 학습 콘텐츠를 자동 생성해 GitHub Pages에 게시하는 파이프라인",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "day:prepare": "node scripts/prepare.js",
    "day:settle": "node scripts/settle.js",
    "day:build": "node scripts/build.js",
    "day:verify": "node scripts/verify.js",
    "day:mock": "node scripts/mock-generate.js",
    "test": "node --test \"tests/**/*.test.js\""
  }
}
